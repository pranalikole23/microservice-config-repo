server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/orderdb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
